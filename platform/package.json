{
  "name": "lh-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Liquidity Hedge Platform — monorepo (see ../product-design/)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
