{
  "name": "@lh/storage",
  "version": "0.1.0",
  "private": true,
  "description": "PostgreSQL storage behind the existing store ports (pool snapshots, candles, append-only event ledgers). Least-privilege roles, append-only grants, parameterized queries only.",
  "main": "src/index.ts",
  "scripts": {
    "test": "ts-mocha 'tests/**/*.test.ts' --timeout 120000 --recursive",
    "typecheck": "tsc --noEmit",
    "migrate": "ts-node src/migrate-cli.ts"
  },
  "dependencies": {
    "@lh/market-data": "workspace:*",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.0.0",
    "@types/pg": "^8.11.0",
    "chai": "^4.3.0",
    "mocha": "^10.2.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
