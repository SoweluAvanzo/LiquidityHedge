{
  "name": "@lh/risk-models",
  "version": "0.1.0",
  "private": true,
  "description": "Pluggable price-model port (AR-5) with GBM and empirical-bootstrap implementations, plus the portfolio Monte-Carlo engine (FR-S1..S6). Models are pure, deterministic (seeded), and I/O-free — the contract that makes third-party model sandboxing possible later (NFR-E1).",
  "main": "src/index.ts",
  "scripts": {
    "test": "ts-mocha 'tests/**/*.test.ts' --timeout 120000 --recursive",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lh/core": "workspace:*",
    "@lh/portfolio": "workspace:*"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.0.0",
    "chai": "^4.3.0",
    "mocha": "^10.2.0",
    "ts-mocha": "^10.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  }
}
